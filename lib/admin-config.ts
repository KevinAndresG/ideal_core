/**
 * Central config for the admin panel.
 *
 * IMPORTANT: ADMIN_BASE_PATH must match the folder name under `app/`
 * (e.g. `app/x9k4f7m2q8/...`). Keep it out of links/sitemaps and don't
 * reference it from public pages.
 */
export const ADMIN_BASE_PATH = "/x9k4f7m2q8";
